# Store screenshots

Drop 1–6 PNG screenshots here, named `phone-1.png`, `phone-2.png`, etc. Required size: **2000×1250** (Raycast Store standard).

Capture them with Raycast itself: in dev mode, press `⌘⇧.` (Cmd+Shift+Period) — this is Raycast's built-in screenshot tool, designed exactly for store submissions. It outputs at the correct resolution.

Suggested shots:
1. Full contact list with search bar focused
2. Single-match auto-dial (just after typing `Call Henrik`)
3. Action panel expanded showing FaceTime / Copy options

Delete this README before publishing.
